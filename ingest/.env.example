# Copy this file to .env and fill in real values.
# GEMINI_API_KEY is supplied via Docker secret (injest/secrets/gemini_api_key.txt) — do NOT add it here.
ES_URL=http://elastic:changeme_in_production@elasticsearch:9200
INDEX_NAME=clm_knowledge_base
REDIS_HOST=redis_cache
REDIS_PORT=6379
QUEUE_NAME=contract_tasks
JOB_TIMEOUT=600
UPLOAD_DIR=/data/uploads
PDF_DIR=./data
